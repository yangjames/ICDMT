
$('#testline').html("lol");
var cb = new Codebird;
/*
var current_url=location.toString();
var query=current_url.match(/\?(.+)$/).split("&amp;");
var parameters = {};
var parameter;

cb.setConsumerKey(twitterConsumerKey,twitterConsumerSecret);
*/
$('#twitter-login-button').click(function() {
	$('#testline').html("lol");
    });
/*
function requestOAuthToken() {
    document.getElementById('testline').value;
    cb.__call(
	"oauth_requestToken",
	{oauth_callback: "tbd"},
	function (reply) {
	    cb.setToken(reply.
*/